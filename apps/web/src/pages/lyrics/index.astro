---
import { getLocaleFromUrl, localizedPath } from '../../i18n';

const locale = getLocaleFromUrl(Astro.url);
const response = Astro.redirect(localizedPath(locale, '/playlist'));
void response;
return response;
---
