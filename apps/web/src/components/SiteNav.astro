---
import Menu from '@lucide/astro/icons/menu';
import { SITE } from '../config/site';

const links = [
  { label: 'Home', href: '#' },
  { label: 'Blog', href: '#' },
  { label: 'Lyrics', href: '#' },
  { label: 'Jobs', href: '#' },
  { label: 'Contact', href: '#' },
];
---

<nav class="fixed top-0 left-0 right-0 z-50 border-b border-neutral-200 glass-header">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 h-14 flex items-center justify-between">
    <a
      href="/"
      class="font-mono text-sm font-semibold tracking-tight hover:opacity-70 transition-opacity"
    >
      {SITE.domain}
    </a>
    <div class="hidden sm:flex items-center gap-6 text-sm font-medium text-neutral-600">
      {links.map((link) => (
        <a
          href={link.href}
          class={link.label === 'Home' ? 'text-neutral-900' : 'hover:text-neutral-900 transition-colors'}
        >
          {link.label}
        </a>
      ))}
    </div>
    <button class="sm:hidden p-1" aria-label="Open menu">
      <Menu class="w-5 h-5 text-neutral-600" />
    </button>
  </div>
</nav>
