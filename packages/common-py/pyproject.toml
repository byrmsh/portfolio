[project]
name = "portfolio-common"
version = "0.1.0"
description = "Shared Python helpers for portfolio apps."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "environs>=14.1.1",
  "redis>=5.2.1",
  "structlog>=25.1.0",
]

[tool.uv]
package = true

