[project]
name = "portfolio-schema"
version = "0.1.0"
description = "Shared Pydantic schemas and Redis key helpers for portfolio apps."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10.6",
]

[tool.uv]
package = true
