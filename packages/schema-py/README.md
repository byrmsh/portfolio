# portfolio-schema

Shared Pydantic schemas and Redis key helpers for the portfolio monorepo.

- `portfolio_schema.dashboard`: UI/dashboard and Redis projection models
- `portfolio_schema.upwork`: raw Upwork response models based on `apps/upworker/typex.py` and `apps/upworker/job-search.gql`
